# Production env file for VPS deploy (Docker Compose)
# Copy to .env.prod on the VPS and replace values.

# GHCR images are published as:
# ghcr.io/<owner>/gameforum-backend:latest
# ghcr.io/<owner>/gameforum-frontend:latest
GHCR_OWNER=SircuMihai

POSTGRES_DB=ForumDataBase
POSTGRES_USER=admin
POSTGRES_PASSWORD=mnbvc

JWT_SECRET=JWT_SECRET:mySecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLongForHS256Algorithm
JWT_EXPIRATION=36000000
